version: '3'

services:
  db:
    image: postgres:12.1-alpine
    ports:
      - 5432:5432
  api:
    build:
      context: .
      args:
        RAILS_ENV: development
    ports:
      - "3000:3000"
    depends_on:
      - db

